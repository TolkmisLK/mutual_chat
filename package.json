{
  "name": "mutual-chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "test": "node --test",
    "build": "vite build --config vite.widget.config.js && vite build && vite build --config vite.embed.config.js",
    "server:local": "node tool/local-server.js",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "matrix-js-sdk": "42.3.0"
  },
  "devDependencies": {
    "@electron/packager": "20.3.0",
    "@playwright/test": "1.63.0",
    "vite": "8.3.0"
  }
}
